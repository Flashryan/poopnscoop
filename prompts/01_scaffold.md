# Phase 1 — Scaffold project (Codex)

Goal: Create the Next.js + TS project with basic pages and wiring.

Tasks:
- Scaffold Next.js App Router project
- Add Tailwind (optional, but prefer for speed)
- Add Prisma + env handling
- Create placeholder pages: /, /pricing, /service-area, /faq, /privacy, /terms, /contact
- Create admin routes: /admin/login, /admin/enquiries

DoD:
- App runs locally
- Lint/test scripts exist
- Prisma connects (no models yet ok)
